# Example Workspace — "data-engineer-demo"

> ⚠️ **Everything here is FICTIONAL.** Candidate *Priya Nadar*, company *Streamline* (a made-up
> analytics platform), projects, and metrics are invented to show the **shape** of a data
> engineer's workspace. Copy the structure, not the content.

Same engine, **data engineering** flavor — the taxonomy covers batch vs streaming, data modeling,
pipeline reliability/idempotency, data quality, distributed compute, and orchestration.

```
data-engineer-demo/
├── profile.md
├── knowledge-base/
│   ├── topics.md              # data-eng taxonomy
│   ├── star-stories.md
│   └── company-styles/Streamline.md
└── data/
    ├── pipeline.md
    ├── capability-profile.md
    └── mock-records/2026-03-04-pipeline-mock.md
```

Try it: `cp -r examples/data-engineer-demo ~/de-demo && cd ~/de-demo`, then `/interview status`.
