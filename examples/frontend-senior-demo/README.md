# Example Workspace — "frontend-senior-demo"

> ⚠️ **Everything here is FICTIONAL.** Candidate *Sam Ito*, company *Canvas* (a made-up design-tool
> SaaS), projects, and metrics are invented to show the **shape** of a frontend engineer's
> workspace. Copy the structure, not the content.

Same engine, **frontend** flavor — the taxonomy covers rendering/reconciliation, state management,
web performance (LCP/INP), accessibility, and frontend system design.

```
frontend-senior-demo/
├── profile.md
├── knowledge-base/
│   ├── topics.md              # frontend taxonomy
│   ├── star-stories.md
│   └── company-styles/Canvas.md
└── data/
    ├── pipeline.md
    ├── capability-profile.md
    └── mock-records/2026-02-10-perf-mock.md
```

Try it: `cp -r examples/frontend-senior-demo ~/fe-demo && cd ~/fe-demo`, then `/interview status`.
